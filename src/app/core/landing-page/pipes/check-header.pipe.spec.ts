import { CheckHeaderPipe } from './check-header.pipe';

describe('CheckHeaderPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckHeaderPipe();
    expect(pipe).toBeTruthy();
  });
});
