import { ProductLicensePipe } from './product-license.pipe';

describe('ProductLicensePipe', () => {
  it('create an instance', () => {
    const pipe = new ProductLicensePipe();
    expect(pipe).toBeTruthy();
  });
});
