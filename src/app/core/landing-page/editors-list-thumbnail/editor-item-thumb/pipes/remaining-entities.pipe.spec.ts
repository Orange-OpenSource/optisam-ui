import { RemainingEntitiesPipe } from './remaining-entities.pipe';

describe('RemainingEntitiesPipe', () => {
  it('create an instance', () => {
    const pipe = new RemainingEntitiesPipe();
    expect(pipe).toBeTruthy();
  });
});
