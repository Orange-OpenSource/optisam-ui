import { TrimTextRangePipe } from './trim-text-range.pipe';

describe('TrimTextRangePipe', () => {
  it('create an instance', () => {
    const pipe = new TrimTextRangePipe();
    expect(pipe).toBeTruthy();
  });
});
