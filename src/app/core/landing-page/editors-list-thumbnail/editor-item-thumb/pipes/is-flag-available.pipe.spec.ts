import { IsFlagAvailablePipe } from './is-flag-available.pipe';

describe('IsFlagAvailablePipe', () => {
  it('create an instance', () => {
    const pipe = new IsFlagAvailablePipe();
    expect(pipe).toBeTruthy();
  });
});
