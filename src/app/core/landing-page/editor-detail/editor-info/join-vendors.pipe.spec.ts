import { JoinVendorsPipe } from './join-vendors.pipe';

describe('JoinVendorsPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinVendorsPipe();
    expect(pipe).toBeTruthy();
  });
});
