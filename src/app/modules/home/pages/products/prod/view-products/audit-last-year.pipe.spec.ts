import { AuditLastYearPipe } from './audit-last-year.pipe';

describe('AuditLastYearPipe', () => {
  it('create an instance', () => {
    const pipe = new AuditLastYearPipe();
    expect(pipe).toBeTruthy();
  });
});
