import { FilterEditorPipe } from './filter-editor.pipe';

describe('FilterEditorPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEditorPipe();
    expect(pipe).toBeTruthy();
  });
});
