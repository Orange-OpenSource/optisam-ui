import { JoinProductVersionPipe } from './join-product-version.pipe';

describe('JoinProductVersionPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinProductVersionPipe();
    expect(pipe).toBeTruthy();
  });
});
