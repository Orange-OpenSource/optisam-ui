import { FilterByWordPipe } from './filter-by-word.pipe';

describe('FilterByWordPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByWordPipe();
    expect(pipe).toBeTruthy();
  });
});
