import { CheckMetricsLengthPipe } from './check-metrics-length.pipe';

describe('CheckMetricsLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckMetricsLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
