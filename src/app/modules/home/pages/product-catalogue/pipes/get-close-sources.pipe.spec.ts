import { GetCloseSourcesPipe } from './get-close-sources.pipe';

describe('GetCloseSourcesPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCloseSourcesPipe();
    expect(pipe).toBeTruthy();
  });
});
