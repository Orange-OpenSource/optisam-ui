import { GetVersionsPipe } from './get-versions.pipe';

describe('GetVersionsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetVersionsPipe();
    expect(pipe).toBeTruthy();
  });
});
