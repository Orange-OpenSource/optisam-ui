import { GetOpenSourcePipe } from './get-open-source.pipe';

describe('GetOpenSourcePipe', () => {
  it('create an instance', () => {
    const pipe = new GetOpenSourcePipe();
    expect(pipe).toBeTruthy();
  });
});
