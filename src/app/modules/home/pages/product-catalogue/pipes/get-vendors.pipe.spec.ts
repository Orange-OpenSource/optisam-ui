import { GetVendorsPipe } from './get-vendors.pipe';

describe('GetVendorsPipe', () => {
  it('create an instance', () => {
    const pipe = new GetVendorsPipe();
    expect(pipe).toBeTruthy();
  });
});
