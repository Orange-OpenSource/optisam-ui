import { PartnerManagerNamePipe } from './partner-manager-name.pipe';

describe('PartnerManagerNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PartnerManagerNamePipe();
    expect(pipe).toBeTruthy();
  });
});
