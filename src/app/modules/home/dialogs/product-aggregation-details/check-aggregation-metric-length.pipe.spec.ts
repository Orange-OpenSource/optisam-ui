import { CheckAggregationMetricLengthPipe } from './check-aggregation-metric-length.pipe';

describe('CheckAggregationMetricLengthPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckAggregationMetricLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
