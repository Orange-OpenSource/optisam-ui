import { TakeFirstPipe } from './take-first.pipe';

describe('TakeFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new TakeFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
