import { AuditLastYearOnlyPipe } from './audit-last-year-only.pipe';

describe('AuditLastYearOnlyPipe', () => {
  it('create an instance', () => {
    const pipe = new AuditLastYearOnlyPipe();
    expect(pipe).toBeTruthy();
  });
});
