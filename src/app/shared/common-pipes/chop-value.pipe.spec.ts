import { ChopValuePipe } from './chop-value.pipe';

describe('ChopValuePipe', () => {
  it('create an instance', () => {
    const pipe = new ChopValuePipe();
    expect(pipe).toBeTruthy();
  });
});
