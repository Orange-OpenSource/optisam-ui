import { FrenchNumberPipe } from './french-number.pipe';

describe('FrenchNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new FrenchNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
