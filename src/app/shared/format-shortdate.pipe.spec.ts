import { FormatShortdatePipe } from './format-shortdate.pipe';

describe('FormatShortdatePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatShortdatePipe();
    expect(pipe).toBeTruthy();
  });
});
